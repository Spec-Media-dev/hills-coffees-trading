Immediate on/off preference — notification channels, listing visibility, admin feature flags.

```jsx
<Switch checked={emailAlerts} onChange={setEmailAlerts} label="Email alerts" description="Order, payment and shipment updates" />
```

Use for settings that apply instantly; use Checkbox inside forms that are submitted.
