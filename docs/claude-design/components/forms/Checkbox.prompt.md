Consent and multi-select control — KYB agreements, filter facets, bulk table selection.

```jsx
<Checkbox checked={agreed} onChange={setAgreed}
  label="I accept the marketplace terms" description="Required before the application can be submitted" />
```
