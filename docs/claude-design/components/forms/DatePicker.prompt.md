Date and date-range control for harvest years, licence expiry, ETA and finance period filters.

```jsx
<Field label="Trade licence expiry"><DatePicker icon={<Icon name="calendar" size={16} />} /></Field>
<DatePicker range icon={<Icon name="calendar" size={16} />} />
```
