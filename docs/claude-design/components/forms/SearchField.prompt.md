Pill-shaped search used in the marketplace toolbar, admin queues and table headers.

```jsx
<SearchField value={q} onChange={setQ} icon={<Icon name="search" size={16} />} placeholder="Search listings, origins, lot IDs" />
```
