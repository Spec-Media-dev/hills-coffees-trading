Loading placeholder that keeps the layout stable — tables, listing grids, KPI rows.

```jsx
<Skeleton variant="card" height={220} />
<Skeleton lines={4} />
```

Sand-toned shimmer only; never a grey skeleton on the cream surface.
