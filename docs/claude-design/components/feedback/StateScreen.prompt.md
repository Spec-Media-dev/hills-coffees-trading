Whole-page access and failure states. Presets carry approved copy — override only when you must.

```jsx
<StateScreen state="suspended" icon={<Icon name="octagon-alert" size={28} />}
  action={<Button>Contact Hills Coffee</Button>} secondaryAction={<Button variant="text">Back to dashboard</Button>} />
```

States: `unauthorized`, `suspended`, `pending`, `offline`, `notFound`, `error`.
