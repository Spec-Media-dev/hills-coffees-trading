Persistent, in-context message — KYB pending banner, locked pricing notice, payment instructions.

```jsx
<InlineAlert tone="warning" title="Verification in review" icon={<Icon name="clock" size={18} />}
  action={<Button size="sm" variant="outline">View application</Button>}>
  You can browse listings, but ordering unlocks once your KYB is approved.
</InlineAlert>
```
