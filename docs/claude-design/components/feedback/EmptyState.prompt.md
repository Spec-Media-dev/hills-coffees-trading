Designed zero state for every list, table and queue.

```jsx
<EmptyState icon={<Icon name="package-open" size={24} />} title="No listings yet"
  message="Create your first lot and submit it for review. Approved lots go live to verified buyers."
  action={<Button>Create listing</Button>} />
```
