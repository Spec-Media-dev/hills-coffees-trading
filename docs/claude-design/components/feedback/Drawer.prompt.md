Side sheet for filters on tablet, row detail peeks and admin review panels.

```jsx
<Drawer open={open} onClose={close} title="Filters"
  footer={<><Button variant="text">Reset</Button><Button>Show 42 lots</Button></>}>…</Drawer>
```
