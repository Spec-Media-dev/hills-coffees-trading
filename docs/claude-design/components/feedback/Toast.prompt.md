Transient confirmation, stacked bottom-inline-end at `--z-toast`.

```jsx
<Toast tone="success" title="Payment confirmed" message="Order HC-2026-0418 moved to Processing."
  icon={<Icon name="check" size={16} />} onDismiss={dismiss} />
```

A toast never carries the only copy of important information — the state also updates in place.
