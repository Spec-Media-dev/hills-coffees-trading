Dashboard metric tile — the figure is Benito, the label is quiet Manrope.

```jsx
<KpiCard icon={<Icon name="package" size={22} />} value="865" label="Available bags" />
<KpiCard icon={<Icon name="triangle-alert" size={22} />} value="1" label="Low-stock offers" onDark />
```

Show live counts only; never seed a KPI with a sample or estimated figure.
