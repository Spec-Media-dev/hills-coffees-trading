Sort key + direction pair that sits beside SearchField and FilterChips.

```jsx
<SortControl value={key} direction={dir} onChange={setKey} onDirectionChange={setDir}
  options={[{value:'newest',label:'Newest'},{value:'price',label:'Price / kg'},{value:'qty',label:'Available quantity'}]} />
```
