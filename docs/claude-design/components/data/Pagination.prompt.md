Page stepper under tables and listing grids. In RTL the arrows flip with the layout.

```jsx
<Pagination page={2} pageCount={8} onPageChange={setPage} totalLabel="Showing 21–40 of 148 listings" />
```
