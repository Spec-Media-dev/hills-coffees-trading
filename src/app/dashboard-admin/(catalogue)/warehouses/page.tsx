import { AdminAreaPlaceholder } from "@/components/admin/area-placeholder";

/** Feature 010 — `/dashboard-admin/warehouses`: declared in `lib/admin/areas.ts`; workflow owned by phase 7 (see the matrix). */
export default function WarehousesPage() {
  return <AdminAreaPlaceholder areaKey="warehouses" />;
}
