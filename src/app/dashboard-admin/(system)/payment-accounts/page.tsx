import { AdminAreaPlaceholder } from "@/components/admin/area-placeholder";

/** Feature 010 — `/dashboard-admin/payment-accounts`: declared in `lib/admin/areas.ts`; workflow owned by phase 9 (see the matrix). */
export default function PaymentAccountsPage() {
  return <AdminAreaPlaceholder areaKey="paymentAccounts" />;
}
