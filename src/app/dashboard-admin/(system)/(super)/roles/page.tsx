import { AdminAreaPlaceholder } from "@/components/admin/area-placeholder";

/** Feature 010 — `/dashboard-admin/roles`: declared in `lib/admin/areas.ts`; workflow owned by phase 9 (see the matrix). */
export default function RolesPage() {
  return <AdminAreaPlaceholder areaKey="roles" />;
}
