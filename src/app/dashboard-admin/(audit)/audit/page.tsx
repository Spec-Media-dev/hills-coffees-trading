import { AdminAreaPlaceholder } from "@/components/admin/area-placeholder";

/** Feature 010 — `/dashboard-admin/audit`: declared in `lib/admin/areas.ts`; workflow owned by phase 8 (see the matrix). */
export default function AuditPage() {
  return <AdminAreaPlaceholder areaKey="audit" />;
}
