import { AdminAreaPlaceholder } from "@/components/admin/area-placeholder";

/** Feature 010 — `/dashboard-admin/shipments`: declared in `lib/admin/areas.ts`; workflow owned by phase 6 (see the matrix). */
export default function ShipmentsPage() {
  return <AdminAreaPlaceholder areaKey="shipments" />;
}
