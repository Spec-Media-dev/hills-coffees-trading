import { AdminAreaPlaceholder } from "@/components/admin/area-placeholder";

/** Feature 010 — `/dashboard-admin/inventory`: declared in `lib/admin/areas.ts`; workflow owned by phase 6 (see the matrix). */
export default function InventoryPage() {
  return <AdminAreaPlaceholder areaKey="inventory" />;
}
