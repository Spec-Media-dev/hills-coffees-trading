import { AdminAreaPlaceholder } from "@/components/admin/area-placeholder";

/** Feature 010 — `/dashboard-admin/organizations`: declared in `lib/admin/areas.ts`; workflow owned by phase 3 (see the matrix). */
export default function OrganizationsPage() {
  return <AdminAreaPlaceholder areaKey="organizations" />;
}
