import { AdminAreaPlaceholder } from "@/components/admin/area-placeholder";

/** Feature 010 — `/dashboard-admin/kyb`: declared in `lib/admin/areas.ts`; workflow owned by phase 3 (see the matrix). */
export default function KybPage() {
  return <AdminAreaPlaceholder areaKey="kyb" />;
}
