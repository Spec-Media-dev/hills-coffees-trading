import { AdminAreaPlaceholder } from "@/components/admin/area-placeholder";

/** Feature 010 — `/dashboard-admin/disputes`: declared in `lib/admin/areas.ts`; workflow owned by phase 4 (see the matrix). */
export default function DisputesPage() {
  return <AdminAreaPlaceholder areaKey="disputes" />;
}
