import { AdminAreaPlaceholder } from "@/components/admin/area-placeholder";

/** Feature 010 — `/dashboard-admin/payouts`: declared in `lib/admin/areas.ts`; workflow owned by phase 5 (see the matrix). */
export default function PayoutsPage() {
  return <AdminAreaPlaceholder areaKey="payouts" />;
}
